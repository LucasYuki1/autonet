# primeira vez no CloudShell
git clone https://github.com/SEU_USUARIO/netconfig.git
cd netconfig
python3 netconfig.py

# nas próximas vezes
cd netconfig && git pull && python3 netconfig.py

# quando alterar e quiser salvar
git add -A && git commit -m "ajustes" && git push
